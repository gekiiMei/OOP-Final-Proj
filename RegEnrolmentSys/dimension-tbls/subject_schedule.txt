CREATE TABLE finals.SUBJECT_SCHEDULE (
    sy              INT            NOT NULL
   ,semester       INT            NOT NULL
   ,college_code    VARCHAR(50)    NOT NULL
   ,block_no        VARCHAR(50)    NOT NULL
   ,subject_code    VARCHAR(50)    NOT NULL
   ,day             VARCHAR(20)   
   ,time            VARCHAR(20)   
   ,room            VARCHAR(50)    
   ,type            VARCHAR(50)  
   ,sequence_no     INT            NOT NULL
   ,faculty_id      INT            NOT NULL
    PRIMARY KEY (sy, semester, block_no, subject_code, sequence_no, college_code),
    FOREIGN KEY (sy, semester, block_no, subject_code, college_code, faculty_id) 
        REFERENCES EMPLOYEE(employee_no)
);

/* does not run */